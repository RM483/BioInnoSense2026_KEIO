# 最終UI品質監査 — App Store直前の磨き込み記録

方針: **機能追加なし・画面構成維持・実描画で判定**。
根拠スクリーンショット: `outputs/webshots/audit/`(監査前) と `after_*`(修正後)。
Web は 320×568 / 390×844 / 430×932 / 1280×800、light/dark、
通常/空/測定中/結果 の実描画で確認した。
Flutter はサンドボックスにSDKがないため**コード監査のみ**(§4残件に明記)。

## 1. 監査で発見し修正した項目 (P1–P8)

| # | 発見(実描画) | 修正 | 前後 |
|---|---|---|---|
| P1 | 空状態ホームの見出しが「…ましょ/う」と1文字折返し(390/dark両方で再現) | `.home-words { text-wrap: balance }` | 12→after_12: 2行均等折返し |
| P2 | 320×568でCTA「測定をはじめる」がフォールド下(主行動が見えない) | max-height:700pxでリング176→140圧縮+縦余白圧縮+CSS orderでCTAをトレンドより先へ(通常レイアウト不変) | 03→after2_03: CTAが1画面内(getBoundingClientRectで機械判定 CTA_IN_VIEWPORT) |
| P3 | Web履歴の空状態が行き止まり(Flutter側はA11で修正済みだったのに未反映) | 「測定をはじめる」導線を追加(タップ領域44px) | 13→after_13 |
| P4 | Web測定中、ウォームアップ中に「安定」と状態を断定(Flutter側F5と同じ欠陥が未反映) | 呼気前は「…」+参考値ppmのみ。behaviorテストも仕様に更新 | 10→after_10 (computed textContent「…」で確認) |
| P5 | 履歴一覧が古い順に見える | **誤検出** — アプリの正準順は新しい順(App.tsxが先頭追加)で、監査seedデータの順序ミスだった。一度入れたreverse()を差し戻し、視点をコードに記録 | — |
| P6 | トレンド最新点のハローが右縁に接する(デスクトップで顕著) | PAD.right 16→20 | 05→(再ビルド反映) |
| P7 | 履歴ヘッダの「7件」— 開発者向けの数(削るレビュー) | 削除。件数は愛犬ページの「これまでの記録」に残る(そこは意味がある) | 06→after_06 |
| P8 | Web接続失敗が `alert()` で生メッセージ("not connected")— エラー画面を作らない原則に反する | 非ブロッキングトースト(role=status/aria-live、reduced-motion対応、タブバー+SafeArea上に配置)+行動語コピーへ | コード+build確認 |

Flutter側の修正:
- **F-A**: Reduce Motion — `MediaQuery.disableAnimations` で測定中の
  呼吸リングを停止し中間値で静止(didChangeDependencies)。

## 2. Apple 4視点レビュー(34項目) — 判定つき

### Apple HIGレビュアー (1–10)
1. 呼気前の状態断定(Web) → **修正**(P4)
2. 小型端末で主CTAが不可視 → **修正**(P2)
3. 空状態の行き止まり(Web履歴) → **修正**(P3)
4. 1文字オーファン折返し → **修正**(P1)
5. `alert()`によるモーダルエラー → **修正**(P8)
6. 破壊的操作「すべて削除」の確認 → **合格確認**(confirm実装済みを実コードで確認)
7. 画面遷移の統一(0.32s cubic-bezier(0.2,0.8,0.2,1) viewIn) → **合格確認**
8. Hero連続性(準備→測定 care-ring) → **合格確認**(Flutter)
9. タブバーのSafe Area(env(safe-area-inset-bottom)) → **合格確認**
10. 押下状態(care-ring scale0.98 / CTA scale+影減) → **合格確認**

### シニアプロダクトデザイナー (11–20)
11. 履歴「7件」は開発者の数字 → **削除**(P7)
12. トレンド点が縁に接し窮屈 → **修正**(P6)
13. 「デモ(実機なし)」表記 → **保留**: デバッグ用の事実であり設定に隔離済み。隠すと実機テスト時に混乱する
14. 履歴のチャートはホームと重複では → **保留**: ホームは7日/履歴は14件+要約文で役割が違う。削除は情報損失
15. 結果画面の3統計(時間/平均/最大) → **保留**: 数値は「証拠」として下部に小さく。docs/17の階層どおり
16. デスクトップの間延び → **許容**: 640px単カラム+ヘッダセグメントで設計内。Webは補助面
17. 履歴行の右端ppmが濃すぎないか → **合格確認**(tertiary)
18. ダークのCTA文字コントラスト → **合格確認**(前フェーズA21で修正済み、rgb(14,15,18)実測)
19. 320でのサブコピー折返し → **修正**(P1のbalanceが同時に解決、after_12で確認)
20. 角丸の統一(カード16/ボタン29/入力14/トースト14) → **合格確認**(トークン準拠)

### アクセシビリティ専門家 (21–28)
21. 測定リングのaria-label → **合格確認**(「測定をはじめる」)
22. トレンドSVGの代替テキスト → **合格確認**(aria-label+要約文が地の文)
23. 状態を色だけで伝えていないか → **合格確認**(チップ=アイコン+語、リング=語+文)
24. prefers-reduced-motion(Web) → **合格確認**(ring/chart/view停止) + トーストにも追加(P8)
25. Reduce Motion(Flutter) → **修正**(F-A)
26. 通知のaria-live → **修正**(P8のトーストにrole=status)
27. Dynamic Type(Flutter: CTA minHeight/タブclamp1.3) → **合格確認**(コード) — 実機描画は残件
28. Webの文字拡大 → **残件**: px指定のためブラウザのフォント設定単独では拡大されない(ページズームは可)。rem移行は全面改修になるため見送り、理由を記録

### 初めて使う一般ユーザー (29–34)
29. 最初に何をすればいいか分かるか → **合格確認**(空状態: 「はじめての測定をしてみましょう」+1回3分の説明+CTA)
30. 接続を意識させられないか → **合格確認**(「はじめる」と同時に接続、を明記)
31. 測定中に不安にならないか → **修正済みの効果確認**(P4: 準備中に「安定」と言われる方が後で混乱する)
32. 失敗したとき責められないか → **合格確認**(E_NO_BREATH→「もういちど呼気を当ててみましょう」)
33. 品質が数字で突きつけられないか → **合格確認**(「測定の質・高い」の語のみ)
34. 削除が怖くないか → **合格確認**(確認ダイアログ+削除は設定の奥)

## 3. 検証結果 (このフェーズ)

- Web: `tsc` / `vitest 3/3` / `vite build` green。実描画スイープ
  (17枚+機械判定2件: CTA_IN_VIEWPORT, ring-word「…」)。
- FWホストテスト: 354アサーション green(変更なしの回帰確認)。
- 完了条件チェック: 全主要画面実描画☑ / light・dark☑ / 小型端末☑ /
  空状態☑ / エラー状態☑(トースト+E_NO_BREATH) / アニメーション☑
  (reduced-motion含む) / A11y☑(上記21–28) / Web build green☑ /
  変更前後比較☑(本書) / 残件記録☑(§4)。

## 4. 残件(正直な未確認一覧)

1. **Flutter analyze/test/build 未実行** — サンドボックスにSDKなし。
   Macで `./tool/verify_app.sh --build` 必須(widget/controllerテストは
   呼気フロー対応済みだが**未実行=未確認**)。
2. Flutterの実機描画(Dynamic Type最大値・VoiceOver実操作・Hero実挙動)。
3. 権限未許可状態(BLE permission拒否)の実機確認 — 文言・導線はコード上
   実装済み。
4. Webの文字サイズ設定追従(px→rem移行) — 見送り理由は§2-28。
5. Flutterホーム空状態の折返し(text-wrap balance相当がないため
   端末幅依存) — 実機で1文字折返しが出た場合は文言側で調整する。
6. iOSアイコン/スプラッシュ実機確認、Android各densityの実機確認。
